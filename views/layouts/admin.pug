doctype html
html(lang="fr")
    head
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        meta(charset="UTF-8")
        title= title || 'admin'
        link(rel="shortcut icon", href="/images/icon.webp", type="image/x-icon")
        link(rel="stylesheet", href="/css/import.css")
        link(rel="stylesheet", href="/css/layouts/admin.css")
        block links
    body
        div.layout
            include ../common/sidebar
            .main
                .admin-nav
                    h2= pageName || "Undefined page"
                    //- .admin__menu
                    //-     .admin__lang
                    //-         .admin__lang-active
                    //-         .admin__lang-panel
                    //-             .lang English
                    //-             .lang French
                    //-             .lang Dutch
                    //-     .admin__profile
                    //-         .admin__profile-image
                    //-         .admin__profile-name
                    #openMenu.flex-c
                        svg(xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="#e3e3e3")
                            path(d="M120-240v-80h720v80H120Zm0-200v-80h720v80H120Zm0-200v-80h720v80H120Z")
                .main-wrapper
                    block content
        script(src="/js/layouts/admin.js" type="module")
        script.
            document.querySelectorAll(".sidebar-header__panel .company")
                .forEach(el => {
                    el.addEventListener("click", async () => {
                    const companyId = el.dataset.id;

                    await fetch("/switch-company", {
                        method: "POST",
                        headers: {
                        "Content-Type": "application/json"
                        },
                        body: JSON.stringify({ companyId })
                    });

                    location.reload();
                    });
                });
        block scripts

        script.
            document.addEventListener("keydown", (e) => {
                if (e.key === "x" || e.key === "X") {
                     document.body.classList.toggle("debug");
                }
            })
            const COMPANY_ID = !{company ? `"${company._id}"` : "null"};



            