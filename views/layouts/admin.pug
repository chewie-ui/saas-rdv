
include ../templates/modals/dialog.pug

doctype html
html(lang="fr")
    head
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        meta(charset="UTF-8")
        title= title || 'BranShee Admin'

        //- Google tag (GA4 + Google Ads)
        script(async src="https://www.googletagmanager.com/gtag/js?id=G-PZR5994GKD")
        script.
            window.dataLayer = window.dataLayer || [];
            function gtag(){dataLayer.push(arguments);}
            gtag('js', new Date());
            gtag('config', 'G-PZR5994GKD');
            gtag('config', 'AW-18287595473');
        link(rel="icon" href="/images/icon.svg" type="image/svg+xml")
        link(rel="shortcut icon" href="/images/icon.svg")
        link(rel="stylesheet", href="/css/import.css")
        link(rel="stylesheet", href="/css/layouts/admin.css")
        link(rel="stylesheet", href="/css/components/admin/guided-tour.css")
        link(rel="preconnect" href="https://fonts.googleapis.com")
        link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin="")
        link(rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=block")
        //- Anti-flash : applique le thème AVANT le rendu
        script.
            (function(){try{var t=localStorage.getItem('branshee-theme');if(t==='dark')document.documentElement.setAttribute('data-theme','dark');}catch(e){}})();
        //- ── Messages du fondateur (superadmin → utilisateur) ──────────────
        style.
            .admin-msgs { display: flex; flex-direction: column; gap: 12px; margin: 0 0 4px; }
            .admin-msgs:empty { display: none; }
            .admin-msg {
                position: relative;
                display: flex;
                gap: 14px;
                padding: 16px 46px 16px 18px;
                border-radius: 14px;
                border: 1px solid var(--am-border, #e2e8f0);
                background: var(--am-bg, #f8fafc);
                animation: amSlideIn .35s cubic-bezier(.16,1,.3,1);
            }
            @keyframes amSlideIn { from { opacity: 0; transform: translateY(-8px); } to { opacity: 1; transform: translateY(0); } }
            .admin-msg__icon { flex-shrink: 0; width: 38px; height: 38px; border-radius: 10px; display: flex; align-items: center; justify-content: center; font-size: 19px; background: var(--am-icon-bg, #fff); border: 1px solid var(--am-border, #e2e8f0); }
            .admin-msg__body { flex: 1; min-width: 0; }
            .admin-msg__title { font-size: 14.5px; font-weight: 700; color: var(--am-title, #0f172a); letter-spacing: -.01em; margin-bottom: 3px; }
            .admin-msg__text { font-size: 13px; line-height: 1.55; color: var(--am-text, #475569); white-space: pre-wrap; word-break: break-word; }
            .admin-msg__cta { display: inline-flex; align-items: center; gap: 6px; margin-top: 12px; padding: 8px 15px; border-radius: 9px; font-size: 12.5px; font-weight: 600; text-decoration: none; color: #fff; background: var(--am-accent, #2563eb); transition: filter .15s; }
            .admin-msg__cta:hover { filter: brightness(1.08); }
            .admin-msg__close { position: absolute; top: 12px; right: 12px; width: 26px; height: 26px; border: none; background: transparent; color: var(--am-text, #94a3b8); cursor: pointer; border-radius: 7px; display: flex; align-items: center; justify-content: center; transition: background .15s, color .15s; }
            .admin-msg__close:hover { background: rgba(0,0,0,.06); color: var(--am-title, #0f172a); }
            .admin-msg__from { font-size: 10.5px; font-weight: 600; text-transform: uppercase; letter-spacing: .06em; color: var(--am-accent, #2563eb); margin-bottom: 6px; display: flex; align-items: center; gap: 6px; }
            .admin-msg--info     { --am-bg:#eff6ff; --am-border:#bfdbfe; --am-icon-bg:#fff; --am-title:#1e3a8a; --am-text:#3b5b8c; --am-accent:#2563eb; }
            .admin-msg--tip      { --am-bg:#faf5ff; --am-border:#e9d5ff; --am-icon-bg:#fff; --am-title:#6b21a8; --am-text:#7e5aa0; --am-accent:#9333ea; }
            .admin-msg--success  { --am-bg:#f0fdf4; --am-border:#bbf7d0; --am-icon-bg:#fff; --am-title:#166534; --am-text:#3f7a53; --am-accent:#16a34a; }
            .admin-msg--warning  { --am-bg:#fffbeb; --am-border:#fde68a; --am-icon-bg:#fff; --am-title:#92400e; --am-text:#946a2a; --am-accent:#d97706; }
            .admin-msg--security { --am-bg:#fef2f2; --am-border:#fecaca; --am-icon-bg:#fff; --am-title:#991b1b; --am-text:#a15353; --am-accent:#dc2626; }
            :root[data-theme="dark"] .admin-msg--info     { --am-bg:#0f1f38; --am-border:#1e3a5f; --am-icon-bg:#0b1626; --am-title:#bfdbfe; --am-text:#8fb2de; }
            :root[data-theme="dark"] .admin-msg--tip      { --am-bg:#1e1233; --am-border:#3b2758; --am-icon-bg:#140b24; --am-title:#e9d5ff; --am-text:#c3a8e0; }
            :root[data-theme="dark"] .admin-msg--success  { --am-bg:#0d2417; --am-border:#1c4530; --am-icon-bg:#08160e; --am-title:#bbf7d0; --am-text:#8fceaa; }
            :root[data-theme="dark"] .admin-msg--warning  { --am-bg:#2a1e08; --am-border:#4d3a14; --am-icon-bg:#1c1405; --am-title:#fde68a; --am-text:#d6b876; }
            :root[data-theme="dark"] .admin-msg--security { --am-bg:#2a1010; --am-border:#5a2020; --am-icon-bg:#1c0a0a; --am-title:#fecaca; --am-text:#e0a0a0; }
            :root[data-theme="dark"] .admin-msg__close:hover { background: rgba(255,255,255,.08); }
        block links
    if isImpersonating
        style.
            body { padding-top: 44px !important; }
            .impersonation-bar { position:fixed;top:0;left:0;right:0;z-index:99999;background:#f97316;color:#fff;padding:0 18px;height:44px;display:flex;align-items:center;justify-content:space-between;font-size:13px;font-weight:600;box-shadow:0 2px 12px rgba(0,0,0,.18);gap:12px; }
            .impersonation-bar__left { display:flex;align-items:center;gap:8px; }
            .impersonation-bar__exit { display:inline-flex;align-items:center;padding:5px 13px;border-radius:7px;border:1.5px solid rgba(255,255,255,.5);color:#fff;font-size:12px;font-weight:700;text-decoration:none;transition:background .15s;white-space:nowrap; }
            .impersonation-bar__exit:hover { background:rgba(255,255,255,.15); }
            body { outline:3px solid #f97316;outline-offset:-3px; }

    body
        if isImpersonating
            .impersonation-bar
                .impersonation-bar__left
                    if isImpersonating.mode === 'supervised'
                        span 👁
                        span Supervision active — Compte de&nbsp;
                        strong= isImpersonating.userEmail
                    else
                        span 🔒
                        span Accès discret — Compte de&nbsp;
                        strong= isImpersonating.userEmail
                a.impersonation-bar__exit(href="/superadmin/exit-impersonation") Quitter ✕

        div.layout(class=moduleMode ? 'layout--module' : '')
            if !moduleMode
                include ../common/sidebar
            .main
                .admin-nav
                    #openMenu.flex-c
                        svg(xmlns="http://www.w3.org/2000/svg" height="22" viewBox="0 -960 960 960" width="22" fill="currentColor")
                            path(d="M120-240v-80h720v80H120Zm0-200v-80h720v80H120Zm0-200v-80h720v80H120Z")
                    h2.admin-nav__title= title || pageName || "Undefined page"
                    .admin-nav__actions
                        -
                            var navCtaMap = {
                                'Appointment':   { label: t.sidebar.new_appointment || 'New appointment', href: '/appointment/new' },
                            }
                            var navCta = navCtaMap[pageName]
                            var calendarUrl = currentCompany ? ('/' + (currentCompany.slug || currentCompany._id)) : '/'
                            var calendarFullUrl = 'https://www.branshee.com' + calendarUrl
                        if pageName === 'Appointment'
                            button.sm-btn.sm-btn--ghost#shareCalendarBtn(type="button" data-url=calendarFullUrl title=t.sidebar.share_calendar || 'Share calendar')
                                svg(viewBox="0 -960 960 960" height="16" width="16" fill="currentColor")
                                    path(d="M680-80q-50 0-85-35t-35-85q0-6 3-28L282-392q-16 15-37 23.5t-45 8.5q-50 0-85-35t-35-85q0-50 35-85t85-35q24 0 45 8.5t37 23.5l281-164q-2-7-2.5-13.5T560-760q0-50 35-85t85-35q50 0 85 35t35 85q0 50-35 85t-85 35q-24 0-45-8.5T598-672L317-508q2 7 2.5 13.5t.5 14.5q0 8-.5 14.5T317-452l281 164q16-15 37-23.5t45-8.5q50 0 85 35t35 85q0 50-35 85t-85 35Z")
                                span= t.sidebar.share_calendar || 'Share'
                            button.sm-btn.sm-btn--ghost#syncAgendaBtn(type="button" title="Synchroniser avec mon agenda")
                                svg(viewBox="0 -960 960 960" height="16" width="16" fill="currentColor")
                                    path(d="M200-80q-33 0-56.5-23.5T120-160v-560q0-33 23.5-56.5T200-800h40v-80h80v80h320v-80h80v80h40q33 0 56.5 23.5T840-720v560q0 33-23.5 56.5T760-80H200Zm0-80h560v-400H200v400Zm0-480h560v-80H200v80Zm0 0v-80 80Zm80 240v-80h400v80H280Zm0 160v-80h280v80H280Z")
                                span Sync agenda
                            button.sm-btn.sm-btn--primary#newApptBtn(type="button")
                                svg(viewBox="0 -960 960 960" height="16" width="16" fill="currentColor")
                                    path(d="M440-440H200v-80h240v-240h80v240h240v80H520v240h-80v-240Z")
                                span= t.sidebar.new_appointment || 'New appointment'
                        else if navCta
                            a.sm-btn.sm-btn--primary(href=navCta.href)
                                svg(viewBox="0 -960 960 960" height="16" width="16" fill="currentColor")
                                    path(d="M440-440H200v-80h240v-240h80v240h240v80H520v240h-80v-240Z")
                                span= navCta.label
                if monthlyLimitReached
                    .limit-toast#limitToast
                        button.limit-toast__close#limitToastClose(type="button" title="Fermer")
                            svg(xmlns="http://www.w3.org/2000/svg" height="14px" viewBox="0 -960 960 960" width="14px" fill="currentColor")
                                path(d="m256-200-56-56 224-224-224-224 56-56 224 224 224-224 56 56-224 224 224 224-56 56-224-224-224 224Z")
                        .limit-toast__icon
                            svg(xmlns="http://www.w3.org/2000/svg" height="20px" viewBox="0 -960 960 960" width="20px" fill="currentColor")
                                path(d="m40-120 440-760 440 760H40Zm138-80h604L480-720 178-200Zm302-40q17 0 28.5-11.5T520-280q0-17-11.5-28.5T480-320q-17 0-28.5 11.5T440-280q0 17 11.5 28.5T480-240Zm-40-120h80v-200h-80v200Zm40-100Z")
                        .limit-toast__body
                            strong Vous n'avez plus de crédit ce mois-ci
                            span Limite de #{monthlyBookingsLimit} rendez-vous atteinte — vos clients ne peuvent plus réserver.
                            a.limit-toast__cta(href="/subscription") Passer au Pro →
                    script.
                        (function() {
                            var toast = document.getElementById('limitToast');
                            var closeBtn = document.getElementById('limitToastClose');
                            if (!toast || !closeBtn) return;
                            // Reste visible sur TOUTES les pages tant qu'il n'est pas fermé —
                            // une fois fermé, on s'en souvient pour le reste de la session de
                            // navigation (réapparaît à la prochaine session si toujours plein).
                            if (sessionStorage.getItem('bs_limit_toast_dismissed') === '1') {
                                toast.style.display = 'none';
                                return;
                            }
                            closeBtn.addEventListener('click', function() {
                                toast.style.display = 'none';
                                sessionStorage.setItem('bs_limit_toast_dismissed', '1');
                            });
                        })();
                //- Messages du fondateur (rendus côté client, voir script en bas)
                #adminMessages.admin-msgs
                .main-wrapper
                    block content
        +dialog()

        //- ── Popup Sync Agenda ──────────────────────────────────────────────
        .sync-popup-overlay#syncPopupOverlay(style="display:none")
            .sync-popup
                .sync-popup__header
                    .sync-popup__title
                        svg(viewBox="0 -960 960 960" height="18" width="18" fill="currentColor")
                            path(d="M200-80q-33 0-56.5-23.5T120-160v-560q0-33 23.5-56.5T200-800h40v-80h80v80h320v-80h80v80h40q33 0 56.5 23.5T840-720v560q0 33-23.5 56.5T760-80H200Zm0-80h560v-400H200v400Zm0-480h560v-80H200v80Zm0 0v-80 80Zm80 240v-80h400v80H280Zm0 160v-80h280v80H280Z")
                        span Synchroniser mon agenda
                    button.sync-popup__close#syncPopupClose(type="button" aria-label="Fermer")
                        svg(viewBox="0 -960 960 960" height="18" width="18" fill="currentColor")
                            path(d="m256-200-56-56 224-224-224-224 56-56 224 224 224-224 56 56-224 224 224 224-56 56-224-224-224 224Z")
                .sync-popup__body
                    -
                        var _gcal = (user && user.googleCalendar) || {}
                    if _gcal.connected
                        .sync-gcal-status.sync-gcal-status--on
                            svg(xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" width="20" height="20")
                                path(fill="#EA4335" d="M24 9.5c3.1 0 5.8 1.1 8 2.9l6-6C34.5 3.1 29.6 1 24 1 14.8 1 7 6.7 3.7 14.7l7 5.4C12.4 14 17.7 9.5 24 9.5z")
                                path(fill="#4285F4" d="M46.1 24.5c0-1.6-.1-3.1-.4-4.5H24v8.5h12.4c-.5 2.8-2.1 5.1-4.4 6.7l7 5.4c4.1-3.8 6.5-9.4 6.5-16.1z")
                                path(fill="#FBBC05" d="M10.7 28.6C10.3 27.4 10 26.2 10 25s.3-2.4.7-3.5l-7-5.4C2.6 18.6 2 21.7 2 25s.6 6.4 1.7 9.2l7-5.6z")
                                path(fill="#34A853" d="M24 47c5.6 0 10.3-1.8 13.7-5l-7-5.4c-1.9 1.3-4.3 2-6.7 2-6.3 0-11.6-4.3-13.3-10l-7 5.4C7 40.3 14.8 47 24 47z")
                                path(fill="none" d="M2 2h44v44H2z")
                            .sync-gcal-status__text
                                strong Google Calendar connecté
                                if _gcal.email
                                    span= _gcal.email
                            svg.sync-gcal-status__check(xmlns="http://www.w3.org/2000/svg" height="18px" viewBox="0 -960 960 960" width="18px" fill="#16a34a")
                                path(d="M382-240 154-468l57-57 171 171 367-367 57 57-424 424Z")
                        p.sync-popup__desc Tous vos rendez-vous (créations, modifications, annulations) sont synchronisés automatiquement et en temps réel avec votre Google Agenda.
                        form#gcalDisconnectForm(method="POST" action="/auth/google/calendar/disconnect")
                            button.sm-btn.sm-btn--ghost.sync-gcal-disconnect(type="submit") Déconnecter Google Calendar
                    else
                        p.sync-popup__desc Connectez votre Google Agenda pour que tous vos rendez-vous (création, modification, annulation) s'y synchronisent automatiquement et en temps réel.
                        a.sync-opt.sync-opt--gcal-connect(href="/auth/google/calendar")
                            .sync-opt__icon.sync-opt__icon--google
                                svg(xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" width="22" height="22")
                                    path(fill="#EA4335" d="M24 9.5c3.1 0 5.8 1.1 8 2.9l6-6C34.5 3.1 29.6 1 24 1 14.8 1 7 6.7 3.7 14.7l7 5.4C12.4 14 17.7 9.5 24 9.5z")
                                    path(fill="#4285F4" d="M46.1 24.5c0-1.6-.1-3.1-.4-4.5H24v8.5h12.4c-.5 2.8-2.1 5.1-4.4 6.7l7 5.4c4.1-3.8 6.5-9.4 6.5-16.1z")
                                    path(fill="#FBBC05" d="M10.7 28.6C10.3 27.4 10 26.2 10 25s.3-2.4.7-3.5l-7-5.4C2.6 18.6 2 21.7 2 25s.6 6.4 1.7 9.2l7-5.6z")
                                    path(fill="#34A853" d="M24 47c5.6 0 10.3-1.8 13.7-5l-7-5.4c-1.9 1.3-4.3 2-6.7 2-6.3 0-11.6-4.3-13.3-10l-7 5.4C7 40.3 14.8 47 24 47z")
                                    path(fill="none" d="M2 2h44v44H2z")
                            .sync-opt__text
                                strong Connecter Google Calendar
                                span Synchronisation automatique en temps réel
                            svg.sync-opt__arrow(xmlns="http://www.w3.org/2000/svg" height="16px" viewBox="0 -960 960 960" width="16px" fill="currentColor")
                                path(d="M504-480 320-664l56-56 240 240-240 240-56-56 184-184Z")

                    //- ── Autres agendas (lecture seule via webcal) ──
                    .sync-popup__divider
                        span Ou abonnement lecture seule (Apple / Outlook)
                    .sync-popup__url-wrap
                        input.sync-popup__url-input#syncFeedUrl(type="text" readonly placeholder="Chargement…")
                        button.sync-popup__copy#syncCopyUrl(type="button" title="Copier le lien")
                            svg(viewBox="0 -960 960 960" height="16" width="16" fill="currentColor")
                                path(d="M360-240q-33 0-56.5-23.5T280-320v-480q0-33 23.5-56.5T360-880h360q33 0 56.5 23.5T800-800v480q0 33-23.5 56.5T720-240H360Zm0-80h360v-480H360v480ZM200-80q-33 0-56.5-23.5T120-160v-560h80v560h440v80H200Zm160-240v-480 480Z")
                    .sync-popup__options
                        a.sync-opt#syncApple(href="#")
                            .sync-opt__icon.sync-opt__icon--apple
                                svg(xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="22" height="22" fill="white")
                                    path(d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.8-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11z")
                            .sync-opt__text
                                strong Apple Calendar
                                span iPhone · iPad · Mac
                        a.sync-opt#syncOutlook(href="#" target="_blank" rel="noopener")
                            .sync-opt__icon.sync-opt__icon--outlook
                                svg(xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" width="22" height="22")
                                    path(fill="#1565C0" d="M29 8H44a1 1 0 011 1v30a1 1 0 01-1 1H29V8z")
                                    path(fill="#1E88E5" d="M5 8H29V40H5z")
                                    path(fill="#fff" d="M17 16c-3.3 0-6 2.7-6 6s2.7 6 6 6 6-2.7 6-6-2.7-6-6-6zm0 10c-2.2 0-4-1.8-4-4s1.8-4 4-4 4 1.8 4 4-1.8 4-4 4z")
                                    path(fill="#fff" d="M33 18h-2v4h-4v2h4v4h2v-4h4v-2h-4z")
                            .sync-opt__text
                                strong Outlook
                                span Outlook.com · Microsoft 365

        //- ── Widget de chat support flottant (disponible partout) ───────────
        .support-widget#supportWidget
            button.support-widget__btn#supportWidgetBtn(type="button" title="Discuter avec nous")
                svg.support-widget__icon--chat(xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="currentColor")
                    path(d="M240-400h480v-80H240v80Zm0-120h480v-80H240v80Zm0-120h480v-80H240v80ZM80-80v-720q0-33 23.5-56.5T160-880h640q33 0 56.5 23.5T880-800v480q0 33-23.5 56.5T800-240H240L80-80Zm126-240h594v-480H160v525l46-45Z")
                svg.support-widget__icon--close(xmlns="http://www.w3.org/2000/svg" height="22px" viewBox="0 -960 960 960" width="22px" fill="currentColor" style="display:none")
                    path(d="m256-200-56-56 224-224-224-224 56-56 224 224 224-224 56 56-224 224 224 224-56 56-224-224-224 224Z")
                span.sb-badge.support-widget__badge#supportWidgetBadge(style="display:none")

            .support-widget__panel#supportWidgetPanel
                .support-widget__header
                    .support-widget__header-text
                        strong Support BranShee
                        span On vous répond le plus vite possible
                    button.support-widget__close#supportWidgetClose(type="button" title="Fermer")
                        svg(xmlns="http://www.w3.org/2000/svg" height="18px" viewBox="0 -960 960 960" width="18px" fill="currentColor")
                            path(d="m256-200-56-56 224-224-224-224 56-56 224 224 224-224 56 56-224 224 224 224-56 56-224-224-224 224Z")
                .support-widget__messages#supportWidgetMessages
                    .support-chat__loading Chargement…
                .support-widget__form
                    textarea.support-widget__input#supportWidgetInput(rows="1" placeholder="Écrivez votre message…")
                    button.support-widget__send#supportWidgetSend(type="button" title="Envoyer")
                        svg(xmlns="http://www.w3.org/2000/svg" height="18px" viewBox="0 -960 960 960" width="18px" fill="currentColor")
                            path(d="M120-160v-640l760 320-760 320Zm80-120 474-200-474-200v140l240 60-240 60v140Z")

        script(src="/js/layouts/admin.js" type="module")
        script(src="/js/components/admin/guided-tour.js")
        script(src="/js/theme.js")
        script(src="/socket.io/socket.io.js")
        script.
            window.__userId = !{user ? JSON.stringify(String(user._id)) : 'null'};
            window.__companyId = !{(company || currentCompany) ? JSON.stringify(String((company || currentCompany)._id)) : 'null'};

            // ── Badge "non lu" (uniquement sur le bouton du widget flottant) ─────
            (function() {
                var widgetBadge = document.getElementById('supportWidgetBadge');
                function render(count) {
                    if (!widgetBadge) return;
                    if (count > 0) { widgetBadge.textContent = count > 99 ? '99+' : count; widgetBadge.style.display = ''; }
                    else { widgetBadge.style.display = 'none'; }
                }
                window.__refreshSupportBadge = function() {
                    fetch('/support/chat/unread-count').then(function(r) { return r.json(); }).then(function(d) { render(d.unread || 0); }).catch(function() {});
                };
                window.__refreshSupportBadge();
            })();

            // ── Connexion socket unique pour toute la page (badge + widget) ──────
            try {
                var socket = io();
                window.__supportSocket = socket;
                socket.on('connect', function() {
                    if (window.__userId) socket.emit('support:joinUser', window.__userId);
                });
                socket.on('support:newMessage', function() { window.__refreshSupportBadge(); });
                document.dispatchEvent(new Event('support:socketReady'));

                // ── Supervision : demande de l'admin ────────────────────────────
                socket.on('supervision:request', function(data) {
                    var modal = document.getElementById('__supervisionModal');
                    if (!modal) return;
                    modal.__token = data.token;
                    modal.style.display = 'flex';
                });
                // ── Supervision : admin connecté → reload pour que la barre server-rendered s'affiche ──
                socket.on('supervision:started', function() {
                    window.location.reload();
                });
                // ── Admin en supervision : rejoindre la room superadmin ──────────
                if (!{JSON.stringify(!!(isImpersonating && isImpersonating.mode === 'supervised'))}) {
                    socket.emit('support:joinAdmin');
                    socket.on('supervision:closed-by-user', function() {
                        window.location.href = '/superadmin/exit-impersonation';
                    });
                }
                // ── Supervision : terminée par l'admin ──────────────────────────
                socket.on('supervision:ended', function() {
                    var bar = document.getElementById('__supervisionBar');
                    if (bar) {
                        bar.innerHTML = '<span style="margin-right:8px">✅</span><span>La supervision est terminée.</span>';
                        setTimeout(function() { window.location.reload(); }, 2000);
                    } else {
                        // Affiche une notif brève si la barre n'est pas visible
                        var notif = document.createElement('div');
                        notif.style.cssText = 'position:fixed;bottom:24px;right:24px;z-index:99999;background:#1f2937;color:#fff;padding:12px 18px;border-radius:10px;font-size:13px;font-weight:600;box-shadow:0 4px 16px rgba(0,0,0,.2)';
                        notif.textContent = '✅ La supervision est terminée.';
                        document.body.appendChild(notif);
                        setTimeout(function() { notif.remove(); }, 3000);
                    }
                });
            } catch (e) {}

            // ── Widget de chat flottant ──────────────────────────────────────────
            (function() {
                var widget   = document.getElementById('supportWidget');
                var btn      = document.getElementById('supportWidgetBtn');
                var closeBtn = document.getElementById('supportWidgetClose');
                var panel    = document.getElementById('supportWidgetPanel');
                var box      = document.getElementById('supportWidgetMessages');
                var input    = document.getElementById('supportWidgetInput');
                var sendBtn  = document.getElementById('supportWidgetSend');
                if (!widget || !btn || !box || !input || !sendBtn) return;

                var iconChat  = widget.querySelector('.support-widget__icon--chat');
                var iconClose = widget.querySelector('.support-widget__icon--close');
                var loaded = false;
                var chatClosed = false;

                function escapeHtml(s) { return s.replace(/</g, '&lt;'); }
                function fmtTime(iso) {
                    var d = new Date(iso);
                    return d.toLocaleDateString('fr-FR', {day:'2-digit', month:'2-digit'}) + ' ' + d.toLocaleTimeString('fr-FR', {hour:'2-digit', minute:'2-digit'});
                }
                function msgHtml(m) {
                    return '<div class="support-chat__msg support-chat__msg--' + m.sender + '">' +
                        escapeHtml(m.text) +
                        '<span class="support-chat__msg-time">' + fmtTime(m.createdAt) + '</span></div>';
                }
                function renderMessages(messages) {
                    if (!messages.length) {
                        box.innerHTML = '<div class="support-chat__empty">Aucun message pour l\'instant — posez votre question !</div>';
                        return;
                    }
                    box.innerHTML = messages.map(msgHtml).join('');
                    box.scrollTop = box.scrollHeight;
                }
                function appendMessage(m) {
                    var empty = box.querySelector('.support-chat__empty');
                    if (empty) empty.remove();
                    box.insertAdjacentHTML('beforeend', msgHtml(m));
                    box.scrollTop = box.scrollHeight;
                }
                function showHint() {
                    if (box.querySelector('.support-chat__hint')) return;
                    box.insertAdjacentHTML('beforeend', '<div class="support-chat__hint">💬 Message envoyé — on vous répond le plus vite possible !</div>');
                    box.scrollTop = box.scrollHeight;
                }

                function showClosedNotice() {
                    chatClosed = true;
                    input.disabled = true;
                    sendBtn.disabled = true;
                    if (box.querySelector('.support-chat__closed')) return;
                    box.insertAdjacentHTML('beforeend',
                        '<div class="support-chat__closed">' +
                        '<p>La conversation a été fermée par l\'administrateur.</p>' +
                        '<div class="support-chat__stars" data-rated="0">' +
                        [1,2,3,4,5].map(function(n) { return '<button type="button" class="support-chat__star" data-star="' + n + '">★</button>'; }).join('') +
                        '</div>' +
                        '<div class="support-chat__rate-thanks" style="display:none">Merci pour votre retour !</div>' +
                        '<button type="button" class="support-chat__restart">🔄 Démarrer une nouvelle conversation</button>' +
                        '</div>'
                    );
                    box.scrollTop = box.scrollHeight;
                }

                function restartChat() {
                    chatClosed = false;
                    input.disabled = false;
                    sendBtn.disabled = false;
                    loaded = false;
                    box.innerHTML = '<div class="support-chat__loading">Chargement…</div>';
                    loadMessages();
                }

                // Délégation : étoiles de notation + relance (créés dynamiquement)
                box.addEventListener('click', function(e) {
                    var star = e.target.closest('.support-chat__star');
                    if (star) {
                        var starsEl = star.parentElement;
                        var value = Number(star.dataset.star);
                        starsEl.querySelectorAll('.support-chat__star').forEach(function(s) {
                            s.classList.toggle('is-active', Number(s.dataset.star) <= value);
                        });
                        starsEl.dataset.rated = '1';
                        fetch('/support/chat/rate', {
                            method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ rating: value })
                        }).catch(function() {});
                        var thanks = starsEl.parentElement.querySelector('.support-chat__rate-thanks');
                        if (thanks) thanks.style.display = '';
                        return;
                    }
                    var restartBtn = e.target.closest('.support-chat__restart');
                    if (restartBtn) restartChat();
                });

                function loadMessages() {
                    fetch('/support/chat').then(function(r) { return r.json(); }).then(function(d) {
                        if (d.success) renderMessages(d.messages);
                        loaded = true;
                    }).catch(function() {
                        box.innerHTML = '<div class="support-chat__empty">Erreur de chargement.</div>';
                    });
                }

                function openWidget() {
                    widget.classList.add('is-open');
                    if (iconChat) iconChat.style.display = 'none';
                    if (iconClose) iconClose.style.display = '';
                    // On lit la conversation en l'ouvrant → plus besoin du badge.
                    var widgetBadge = document.getElementById('supportWidgetBadge');
                    if (widgetBadge) widgetBadge.style.display = 'none';
                    if (!loaded) loadMessages();
                }
                function closeWidget() {
                    widget.classList.remove('is-open');
                    if (iconChat) iconChat.style.display = '';
                    if (iconClose) iconClose.style.display = 'none';
                }
                window.openSupportChat = openWidget;

                btn.addEventListener('click', function() {
                    widget.classList.contains('is-open') ? closeWidget() : openWidget();
                });
                if (closeBtn) closeBtn.addEventListener('click', closeWidget);

                function send() {
                    if (chatClosed) return;
                    var text = input.value.trim();
                    if (!text) return;
                    sendBtn.disabled = true;
                    fetch('/support/chat', {
                        method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ text })
                    }).then(function(r) { return r.json(); }).then(function(d) {
                        sendBtn.disabled = false;
                        if (d.success) {
                            input.value = '';
                            appendMessage({ sender: 'user', text: text, createdAt: new Date().toISOString() });
                            showHint();
                        }
                    }).catch(function() { sendBtn.disabled = false; });
                }
                sendBtn.addEventListener('click', send);
                input.addEventListener('keydown', function(e) {
                    if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); send(); }
                });

                function bindRealtime(socket) {
                    socket.on('support:newMessage', function(payload) {
                        if (!widget.classList.contains('is-open')) return;
                        var hint = box.querySelector('.support-chat__hint');
                        if (hint) hint.remove();
                        if (payload && payload.text) appendMessage(payload);
                        else loadMessages();
                    });
                    socket.on('support:chatClosed', function() {
                        showClosedNotice();
                    });
                }
                if (window.__supportSocket) bindRealtime(window.__supportSocket);
                else document.addEventListener('support:socketReady', function() { bindRealtime(window.__supportSocket); }, { once: true });
            })();
        
        block scripts

        //- ── Modal supervision (demande de l'admin) ─────────────────────────────
        #__supervisionModal(style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.55);z-index:99998;align-items:center;justify-content:center;padding:16px;")
            div(style="background:#fff;border-radius:18px;padding:28px;max-width:400px;width:100%;box-shadow:0 24px 48px rgba(0,0,0,.22);")
                div(style="display:flex;align-items:center;gap:10px;margin-bottom:16px")
                    div(style="width:42px;height:42px;border-radius:50%;background:#fff7ed;border:1px solid #fed7aa;display:flex;align-items:center;justify-content:center;font-size:20px;flex-shrink:0") 👁
                    div
                        p(style="font-size:15px;font-weight:700;color:#111;margin:0") Demande de supervision
                        p(style="font-size:12px;color:#6b7280;margin:0") BranShee Admin
                p(style="font-size:13.5px;color:#374151;line-height:1.55;margin:0 0 20px") Un administrateur BranShee demande l'accès à votre compte pour vous assister. Vous pouvez accepter ou refuser cette demande.
                div(style="display:flex;gap:8px")
                    button#__supervisionAccept(type="button" style="flex:1;padding:10px;border-radius:10px;border:none;background:#f97316;color:#fff;font-weight:700;font-size:14px;cursor:pointer;font-family:inherit") ✓ Autoriser
                    button#__supervisionDecline(type="button" style="flex:1;padding:10px;border-radius:10px;border:1px solid #e5e7eb;background:#f9fafb;color:#4b5563;font-weight:600;font-size:14px;cursor:pointer;font-family:inherit") ✕ Refuser

        //- ── Barre de supervision active (server-rendered côté client uniquement) ──
        //- isImpersonating = côté admin qui supervise → ne pas afficher la barre client
        if isBeingSupervised && !isImpersonating
            #__supervisionBar(style="position:fixed;bottom:0;left:0;right:0;z-index:99997;background:#f97316;color:#fff;padding:10px 20px;display:flex;align-items:center;justify-content:space-between;gap:12px;font-size:13px;font-weight:600;box-shadow:0 -2px 12px rgba(0,0,0,.15);")
                span 👁&nbsp; Un administrateur BranShee consulte actuellement votre compte et procède à des modifications.
                button#__supervisionCloseBtn(type="button" style="background:rgba(255,255,255,.2);border:1.5px solid rgba(255,255,255,.5);color:#fff;font-size:12px;font-weight:700;padding:5px 13px;border-radius:7px;cursor:pointer;white-space:nowrap;font-family:inherit;") Fermer la supervision ✕
        else if !isImpersonating
            #__supervisionBar(style="display:none;position:fixed;bottom:0;left:0;right:0;z-index:99997;background:#f97316;color:#fff;padding:10px 20px;align-items:center;justify-content:space-between;gap:12px;font-size:13px;font-weight:600;box-shadow:0 -2px 12px rgba(0,0,0,.15);")

        script.
            // ── Fermeture de supervision par l'utilisateur ──────────────────────
            (function() {
                var closeBtn = document.getElementById('__supervisionCloseBtn');
                if (!closeBtn) return;
                closeBtn.addEventListener('click', function() {
                    closeBtn.disabled = true;
                    closeBtn.textContent = '⏳…';
                    fetch('/api/supervision/close', { method: 'POST' })
                        .then(function() {
                            var bar = document.getElementById('__supervisionBar');
                            if (bar) bar.innerHTML = '<span>✅ Supervision fermée.</span>';
                            setTimeout(function() { window.location.reload(); }, 1500);
                        })
                        .catch(function() { closeBtn.disabled = false; closeBtn.textContent = 'Fermer la supervision ✕'; });
                });
            })();

        script.
            // ── Supervision modal interactions ──────────────────────────────────
            (function() {
                var modal   = document.getElementById('__supervisionModal');
                var acceptBtn = document.getElementById('__supervisionAccept');
                var declineBtn = document.getElementById('__supervisionDecline');
                if (!modal || !acceptBtn || !declineBtn) return;

                function respond(action) {
                    var token = modal.__token;
                    if (!token) return;
                    modal.style.display = 'none';
                    acceptBtn.disabled = declineBtn.disabled = true;
                    fetch('/api/supervision/respond', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ token: token, action: action }),
                    }).catch(function() {});
                    acceptBtn.disabled = declineBtn.disabled = false;
                }

                acceptBtn.addEventListener('click', function() { respond('accept'); });
                declineBtn.addEventListener('click', function() { respond('decline'); });
            })();

        script.
            const COMPANY_ID = !{(company || currentCompany) ? `"${(company || currentCompany)._id}"` : "null"};
            window.__t = !{JSON.stringify(t.js)};
            (function() {
                var btn = document.getElementById('shareCalendarBtn');
                if (!btn) return;
                btn.addEventListener('click', function() {
                    var url = btn.dataset.url;
                    navigator.clipboard.writeText(url).then(function() {
                        var span = btn.querySelector('span');
                        var orig = span.textContent;
                        span.textContent = '✓ Copié !';
                        setTimeout(function() { span.textContent = orig; }, 2000);
                    });
                });
            })();

            // ── Sync Agenda popup ────────────────────────────────────────────
            (function() {
                var syncBtn     = document.getElementById('syncAgendaBtn');
                var overlay     = document.getElementById('syncPopupOverlay');
                var closeBtn    = document.getElementById('syncPopupClose');
                var feedUrlInput= document.getElementById('syncFeedUrl');
                var copyBtn     = document.getElementById('syncCopyUrl');
                var syncApple   = document.getElementById('syncApple');
                var syncOutlook = document.getElementById('syncOutlook');
                var feedUrl     = null;

                if (!syncBtn) return;

                // Charger le token feed (une fois)
                async function loadFeedUrl() {
                    if (feedUrl) return feedUrl;
                    try {
                        var res  = await fetch('/api/calendar-feed-token');
                        var data = await res.json();
                        feedUrl  = data.feedUrl;
                        return feedUrl;
                    } catch(e) { return null; }
                }

                syncBtn.addEventListener('click', async function() {
                    overlay.style.display = 'flex';
                    document.body.style.overflow = 'hidden';
                    var url = await loadFeedUrl();
                    if (!url) { feedUrlInput.placeholder = 'Erreur — rechargez la page'; return; }

                    feedUrlInput.value = url;

                    var webcalUrl = url.replace(/^https?:\/\//, 'webcal://');

                    // Apple Calendar — webcal:// ouvre directement Calendrier sur macOS/iOS
                    var ua = navigator.userAgent || '';
                    var isApple = /iPhone|iPad|iPod/i.test(ua) || (/Macintosh/i.test(ua) && !/Windows/i.test(ua));
                    if (isApple) {
                        syncApple.href = webcalUrl;
                        syncApple.removeAttribute('target');
                    } else {
                        // Sur Windows/Android : cacher Apple Calendar
                        var appleEl = syncApple.closest('.sync-opt') || syncApple;
                        appleEl.style.display = 'none';
                    }

                    // Outlook Web — abonnement via URL (Outlook.com prend webcal)
                    syncOutlook.href = 'https://outlook.live.com/calendar/0/addfromweb?url=' + encodeURIComponent(webcalUrl) + '&name=BranShee+Agenda';
                });

                // Copier l'URL
                if (copyBtn) {
                    copyBtn.addEventListener('click', function() {
                        if (!feedUrlInput.value) return;
                        navigator.clipboard.writeText(feedUrlInput.value).then(function() {
                            var origTitle = copyBtn.title;
                            copyBtn.style.color = '#16a34a';
                            copyBtn.title = 'Copié !';
                            setTimeout(function() { copyBtn.style.color = ''; copyBtn.title = origTitle; }, 2000);
                        });
                    });
                }

                function closePopup() { overlay.style.display = 'none'; document.body.style.overflow = ''; }
                if (closeBtn) closeBtn.addEventListener('click', closePopup);
                if (overlay)  overlay.addEventListener('click', function(e) { if (e.target === overlay) closePopup(); });
                document.addEventListener('keydown', function(e) { if (e.key === 'Escape' && overlay.style.display !== 'none') closePopup(); });
            })();

            // ── Google Ads conversion tracking ───────────────────────────────
            (function() {
                if (new URLSearchParams(window.location.search).get('gads_conversion') !== '1') return;
                if (typeof gtag !== 'function') return;
                gtag('event', 'conversion', { 'send_to': 'AW-18287595473/oJ11CPzym8gcENGfmpBE' });
                // Nettoyer le param de l'URL sans recharger la page
                var url = new URL(window.location.href);
                url.searchParams.delete('gads_conversion');
                history.replaceState(null, '', url.toString());
            })();

        //- ── Rendu des messages du fondateur (superadmin → utilisateur) ──────
        script.
            (function() {
                var box = document.getElementById('adminMessages');
                if (!box || !window.__userId) return;

                var ICONS = { info: 'ℹ️', tip: '💡', success: '✅', warning: '⚠️', security: '🔒' };
                var FROM  = { info: 'Message de BranShee', tip: 'Conseil BranShee', success: 'BranShee', warning: 'Important', security: 'Sécurité de votre compte' };

                function esc(s) {
                    return String(s == null ? '' : s)
                        .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
                        .replace(/"/g, '&quot;');
                }
                function safeUrl(u) {
                    u = String(u || '').trim();
                    if (/^https?:\/\//i.test(u) || u.charAt(0) === '/') return u;
                    return '';
                }

                function dismiss(id, el) {
                    el.style.transition = 'opacity .2s, transform .2s';
                    el.style.opacity = '0';
                    el.style.transform = 'translateY(-6px)';
                    setTimeout(function() { el.remove(); }, 200);
                    fetch('/account/my-messages/' + id + '/dismiss', { method: 'POST' }).catch(function() {});
                }

                function render(messages) {
                    var have = {};
                    Array.prototype.forEach.call(box.querySelectorAll('[data-msg-id]'), function(el) { have[el.getAttribute('data-msg-id')] = true; });
                    (messages || []).forEach(function(m) {
                        var id = String(m._id);
                        if (have[id]) return;
                        var type = ['info','tip','success','warning','security'].indexOf(m.type) >= 0 ? m.type : 'info';
                        var el = document.createElement('div');
                        el.className = 'admin-msg admin-msg--' + type;
                        el.setAttribute('data-msg-id', id);
                        var cta = '';
                        var url = safeUrl(m.ctaUrl);
                        if (m.ctaLabel && url) {
                            cta = '<a class="admin-msg__cta" href="' + esc(url) + '">' + esc(m.ctaLabel) + ' →</a>';
                        }
                        el.innerHTML =
                            '<div class="admin-msg__icon">' + (ICONS[type] || 'ℹ️') + '</div>' +
                            '<div class="admin-msg__body">' +
                                '<div class="admin-msg__from">' + (ICONS[type] || '') + ' ' + esc(FROM[type] || 'Message') + '</div>' +
                                '<div class="admin-msg__title">' + esc(m.title) + '</div>' +
                                '<div class="admin-msg__text">' + esc(m.body) + '</div>' +
                                cta +
                            '</div>' +
                            '<button class="admin-msg__close" type="button" aria-label="Fermer">' +
                                '<svg viewBox="0 -960 960 960" height="16" width="16" fill="currentColor"><path d="m256-200-56-56 224-224-224-224 56-56 224 224 224-224 56 56-224 224 224 224-56 56-224-224-224 224Z"/></svg>' +
                            '</button>';
                        el.querySelector('.admin-msg__close').addEventListener('click', function() { dismiss(id, el); });
                        box.appendChild(el);
                    });
                }

                function load() {
                    fetch('/account/my-messages')
                        .then(function(r) { return r.json(); })
                        .then(function(d) { render(d.messages || []); })
                        .catch(function() {});
                }

                load();
                // Rafraîchissement live via le socket partagé (émis par le superadmin).
                function hook() {
                    if (window.__supportSocket) {
                        window.__supportSocket.on('adminMessage:new', load);
                        return true;
                    }
                    return false;
                }
                if (!hook()) document.addEventListener('support:socketReady', hook);
            })();
