.header
    .header-container.container
        .company
            .logo
                img(src="/images/icon.svg")
            .name Bookelio
        .menu
            .actions
                if user 
                    //- a(href="/logout").btn.secondary Logout
                    a(href="/panel").btn.secondary Dashboard
                    a(href="/logout").btn.primary Logout
                else
                    a(href="/login").btn.secondary Login
                    a(href="/register").btn.primary Register